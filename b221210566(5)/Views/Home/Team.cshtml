@model List< Employee> 
<head>
    <link href="@Url.Content("~/css/Team.css")" rel="stylesheet" type="text/css" />
</head>
<div class="containers">
    <div class="con-1">
        @foreach(var employee in Model)
        {
            
            <h3> @employee.FirstName  @employee.LastName</h3>
            <h3> @employee.Dep?.Name </h3>

        }
    </div>

</div>
